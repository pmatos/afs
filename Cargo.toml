[package]
name = "afs"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
notify = { version = "8.2.0", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
